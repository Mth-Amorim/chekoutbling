

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: box-check-joy-app
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
